%% Generic function for accumulating features (at the location level)
% fGetFeats - specific feature
% cellData - where to collect the data from
function feats = getLchFeats(fGetFeats,cellData)
feats = fGetFeats(cellData);
end